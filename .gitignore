# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Vendored comparison project (untracked nested git repo)
code-context-engine/

# Local fastapi clone for hard A/B tasks (pinned commit 4903347, not cloned per run)
local_fastapi/

# Local fiber/django clones for hard A/B tasks (pinned commits, not cloned per run)
local_fiber/
local_django/

# Local index + coverage artifacts (never meant to be committed)
.ckg/
.coverage
.pytest_cache/

# Root-level scratch dir, distinct from the tracked benchmarks/results/
# (anchored so this does NOT also match benchmarks/results/)
/results/
